# Pihole DNS Configuration
A quick script to automatically change dns settings for a preset network, and DNS servers. 
Useful if your DNS gets changed upon reboot, or otherwise often, for some reason.
